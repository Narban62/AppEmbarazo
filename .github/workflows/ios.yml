name: Build iOS IPA (unsigned, iOS 13)

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build-ios:
    runs-on: macos-14
    defaults:
      run:
        shell: bash

    steps:
      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_15.3.app

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'
          architecture: arm64

      - name: Update Firebase dependencies to stable versions
        run: |
          # Actualizar las versiones en pubspec.yaml con sed, o añadir si no existen
          sed -i '' -E "s|(firebase_core:).*|\1 ^2.30.0|" pubspec.yaml || echo "firebase_core: ^2.30.0" >> pubspec.yaml
          sed -i '' -E "s|(firebase_auth:).*|\1 ^4.17.8|" pubspec.yaml || echo "firebase_auth: ^4.17.8" >> pubspec.yaml
          sed -i '' -E "s|(firebase_analytics:).*|\1 ^10.10.6|" pubspec.yaml || echo "firebase_analytics: ^10.10.6" >> pubspec.yaml
          sed -i '' -E "s|(firebase_messaging:).*|\1 ^14.9.1|" pubspec.yaml || echo "firebase_messaging: ^14.9.1" >> pubspec.yaml
          sed -i '' -E "s|(firebase_storage:).*|\1 ^11.6.10|" pubspec.yaml || echo "firebase_storage: ^11.6.10" >> pubspec.yaml
          sed -i '' -E "s|(cloud_firestore:).*|\1 ^4.17.5|" pubspec.yaml || echo "cloud_firestore: ^4.17.5" >> pubspec.yaml

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Set platform iOS 13 in Podfile
        run: |
          if grep -q '^platform :ios' ios/Podfile; then
            sed -i '' "s/^platform :ios.*/platform :ios, '13.0'/" ios/Podfile
          else
            echo "platform :ios, '13.0'" | cat - ios/Podfile > ios/Podfile.tmp && mv ios/Podfile.tmp ios/Podfile
          fi

      - name: Clean CocoaPods and install dependencies
        run: |
          cd ios
          rm -rf Pods Podfile.lock
          pod install --repo-update
          cd ..

      - name: Set iOS minimum version in Info.plist
        run: |
          INFO_PLIST="ios/Runner/Info.plist"
          if grep -q "<key>MinimumOSVersion</key>" "$INFO_PLIST"; then
            # Reemplaza el valor de MinimumOSVersion sin romper el XML
            sed -i '' 's|<key>MinimumOSVersion</key>\n\s*<string>.*</string>|<key>MinimumOSVersion</key>\n\t<string>13.0</string>|' "$INFO_PLIST"
          else
            # Inserta MinimumOSVersion justo debajo de <dict> si no existe
            sed -i '' '/<dict>/a\
            \ \ \ \ <key>MinimumOSVersion</key>\
            \ \ \ \ <string>13.0</string>' "$INFO_PLIST"
          fi

      - name: Build iOS (release, no codesign)
        run: flutter build ios --release --no-codesign

      - name: Create .ipa from .app
        run: |
          mkdir -p Payload
          cp -r build/ios/iphoneos/Runner.app Payload/
          zip -r app.ipa Payload
          mkdir -p output
          mv app.ipa output/

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: unsigned-ipa
          path: output/app.ipa

